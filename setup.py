from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is a demo repo for developing GPT model from scratch',
    author='Sina Esmaeili',
    license='',
)
